export type ExpenseItem = {
  id: string,
  title: string,
  amount: number,
  category: string,
}

export type Location = {
  id: string;
  place: string;
  country: string;
}

export type User = {
  id: string,
  name: string,
}